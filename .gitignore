.DS_Store
*.log
.env
.env.*
node_modules/
out/
cache/
broadcast/**/dry-run/
__pycache__/
*.pyc

.venv/
.local/*.local.md

# Aderyn writes its report next to the project; the triage that matters is
# committed in docs/static-analysis.md, the raw report is a build artifact.
report.json
aderyn-report.json

# Working material, not part of the project: agent instructions, project-local
# skills and the chat archives live only in the local checkout.
development.md
.local/
chat-archives/
