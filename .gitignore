.DS_Store
*.log
.env
.env.*
node_modules/
out/
cache/
# Foundry broadcast records are operator-local run artifacts: the canonical
# deployment record, with explorer links, lives in docs/deployments.md.
broadcast/
__pycache__/
*.pyc

.venv/

# Aderyn writes its report next to the project; the triage that matters is
# committed in docs/static-analysis.md, the raw report is a build artifact.
report.json
aderyn-report.json

# Editor and working notes stay in the local checkout. Contributors who keep
# such files should add their own patterns to .git/info/exclude rather than
# here: what belongs in this file is what every clone must ignore.
chat-archives/
